{
  "NEXTAUTH_URL": {
    "name": "NEXTAUTH_URL",
    "defaultValue": "https://${{RAILWAY_PUBLIC_DOMAIN}}",
    "description": "Public URL of this Langfuse deployment. Auto-resolves to https://<railway-domain> so OAuth callbacks and email links work out of the box.",
    "isOptional": false
  },
  "DATABASE_URL": {
    "name": "DATABASE_URL",
    "defaultValue": "postgresql://${{postgres.POSTGRES_USER}}:${{postgres.POSTGRES_PASSWORD}}@${{postgres.RAILWAY_PRIVATE_DOMAIN}}:5432/${{postgres.POSTGRES_DB}}",
    "description": "PostgreSQL connection string. Auto-wired to the companion Postgres service via Railway private networking.",
    "isOptional": false
  },
  "SALT": {
    "name": "SALT",
    "defaultValue": "${{secret(32)}}",
    "description": "Salt used for hashing API keys. Auto-generated by Railway at deploy time.",
    "isOptional": false
  },
  "ENCRYPTION_KEY": {
    "name": "ENCRYPTION_KEY",
    "defaultValue": "${{secret(32)}}",
    "description": "64-character hex key used to encrypt sensitive data at rest. Auto-generated by Railway at deploy time. Generate manually with: openssl rand -hex 32",
    "isOptional": false
  },
  "CLICKHOUSE_URL": {
    "name": "CLICKHOUSE_URL",
    "defaultValue": "http://${{clickhouse.RAILWAY_PRIVATE_DOMAIN}}:8123",
    "description": "ClickHouse HTTP endpoint. Auto-wired to the companion ClickHouse service via Railway private networking.",
    "isOptional": false
  },
  "CLICKHOUSE_USER": {
    "name": "CLICKHOUSE_USER",
    "defaultValue": "clickhouse",
    "description": "ClickHouse username. Must match the companion ClickHouse service's CLICKHOUSE_USER.",
    "isOptional": false
  },
  "CLICKHOUSE_PASSWORD": {
    "name": "CLICKHOUSE_PASSWORD",
    "defaultValue": "${{secret(32)}}",
    "description": "ClickHouse password. Must match the companion ClickHouse service's CLICKHOUSE_PASSWORD.",
    "isOptional": false
  },
  "REDIS_HOST": {
    "name": "REDIS_HOST",
    "defaultValue": "${{redis.RAILWAY_PRIVATE_DOMAIN}}",
    "description": "Redis hostname. Auto-wired to the companion Redis service via Railway private networking.",
    "isOptional": false
  },
  "REDIS_AUTH": {
    "name": "REDIS_AUTH",
    "defaultValue": "${{secret(32)}}",
    "description": "Redis password. Must match the companion Redis service's REDIS_AUTH.",
    "isOptional": false
  },
  "LANGFUSE_S3_EVENT_UPLOAD_ENDPOINT": {
    "name": "LANGFUSE_S3_EVENT_UPLOAD_ENDPOINT",
    "defaultValue": "http://${{minio.RAILWAY_PRIVATE_DOMAIN}}:9000",
    "description": "MinIO S3 endpoint for event uploads. Auto-wired to the companion MinIO service via Railway private networking.",
    "isOptional": false
  },
  "LANGFUSE_S3_MEDIA_UPLOAD_ENDPOINT": {
    "name": "LANGFUSE_S3_MEDIA_UPLOAD_ENDPOINT",
    "defaultValue": "http://${{minio.RAILWAY_PRIVATE_DOMAIN}}:9000",
    "description": "MinIO S3 endpoint for media uploads. Auto-wired to the companion MinIO service via Railway private networking.",
    "isOptional": false
  },
  "LANGFUSE_S3_EVENT_UPLOAD_ACCESS_KEY_ID": {
    "name": "LANGFUSE_S3_EVENT_UPLOAD_ACCESS_KEY_ID",
    "defaultValue": "minio",
    "description": "MinIO access key ID for event uploads. Must match the companion MinIO service's MINIO_ROOT_USER.",
    "isOptional": false
  },
  "LANGFUSE_S3_EVENT_UPLOAD_SECRET_ACCESS_KEY": {
    "name": "LANGFUSE_S3_EVENT_UPLOAD_SECRET_ACCESS_KEY",
    "defaultValue": "${{secret(32)}}",
    "description": "MinIO secret access key for event uploads. Must match the companion MinIO service's MINIO_ROOT_PASSWORD.",
    "isOptional": false
  },
  "LANGFUSE_S3_MEDIA_UPLOAD_ACCESS_KEY_ID": {
    "name": "LANGFUSE_S3_MEDIA_UPLOAD_ACCESS_KEY_ID",
    "defaultValue": "minio",
    "description": "MinIO access key ID for media uploads. Must match the companion MinIO service's MINIO_ROOT_USER.",
    "isOptional": false
  },
  "LANGFUSE_S3_MEDIA_UPLOAD_SECRET_ACCESS_KEY": {
    "name": "LANGFUSE_S3_MEDIA_UPLOAD_SECRET_ACCESS_KEY",
    "defaultValue": "${{secret(32)}}",
    "description": "MinIO secret access key for media uploads. Must match the companion MinIO service's MINIO_ROOT_PASSWORD.",
    "isOptional": false
  },
  "TELEMETRY_ENABLED": {
    "name": "TELEMETRY_ENABLED",
    "defaultValue": "true",
    "description": "Enable anonymous telemetry. Set to 'false' to opt out.",
    "isOptional": true
  },
  "LANGFUSE_ENABLE_EXPERIMENTAL_FEATURES": {
    "name": "LANGFUSE_ENABLE_EXPERIMENTAL_FEATURES",
    "defaultValue": "false",
    "description": "Enable experimental features. Set to 'true' to try new features before they are stable.",
    "isOptional": true
  }
}
